import { describe, it, expect } from 'vitest'

describe('useTranscricao', () => {
  it('é um hook que será testado integradamente', () => {
    // Testes de hook com estado requerem renderHook do @testing-library/react
    // Que será adicionado em uma próxima versão do projeto
    // Por enquanto, os testes de validação de arquivo estão no UploadAudio
    expect(true).toBe(true)
  })
})
